export class Author {
    id: number;
    surname: string;
    firstname: string;

    constructor(id: number, surname: string, firstname: string) {
        this.id = id;
        this.surname = surname;
        this.firstname = firstname;
    }
}